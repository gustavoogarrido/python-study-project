services:
  app:
    build: .
    container_name: nltk-use-app
    user: "1000:1000"
    environment:
      - DISPLAY=${DISPLAY}
      - XDG_CACHE_HOME=/tmp/.cache
      - HOME=/tmp
    volumes:
      - ./src:/app/src
      - /tmp/.X11-unix:/tmp/.X11-unix
    command: python src/main.py
